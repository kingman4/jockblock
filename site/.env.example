# Stripe API Keys
# Get these from: https://dashboard.stripe.com/apikeys
#
# IMPORTANT: Never commit actual keys to the repository!
# Copy this file to .env and fill in your real keys.

# Publishable key (safe for client-side)
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Secret key (server-side only - NEVER expose this)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Webhook secret (from Stripe Dashboard -> Webhooks)
# Get this after creating the webhook endpoint
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Product price in cents (1999 = $19.99)
STRIPE_PRICE_AMOUNT=1999

# Your domain (used for success/cancel URLs)
SITE_URL=http://localhost:3000

# Email Service (Resend)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# From email address (must be verified in Resend)
FROM_EMAIL=noreply@jockblock.com

# Admin email (receives review approval requests)
ADMIN_EMAIL=admin@jockblock.com

# Review approval secret (generate a random string)
# Used to secure the approve/reject links
REVIEW_APPROVAL_SECRET=your_random_secret_here

# ===========================================
# PRESALE MODE
# ===========================================
# Client-side: Edit js/site-config.js to set MARKET_DATE
# Server-side: Set this env var for presale order emails
MARKET_DATE=2026-03-01

# When current date <= MARKET_DATE:
# - Buttons show "Pre-order Now" (client-side)
# - Orders receive presale confirmation email (server-side)
# Set to past date or remove to disable presale mode
