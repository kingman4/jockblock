/**
 * Site Configuration
 *
 * MARKET_DATE: Set to a date string (YYYY-MM-DD) to enable presale mode.
 * When current date <= MARKET_DATE, the site shows presale messaging.
 * Set to null or past date to disable presale mode.
 */
export const MARKET_DATE = '2026-03-01'; // Change this to your launch date
